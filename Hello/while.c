#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	int inicializacao = 1;
	
	while(inicializacao <= 10){
		printf("%d \n", inicializacao);
		inicializacao++;
	}
	
	return 0;
}
